# NOTE: the production service was created manually in the dashboard under
# the name "flusk" (flusk.onrender.com); this blueprint mirrors that setup
# for reference / re-creation. Applying it as-is would create parallel
# services named flusk-api / flusk-redis.
services:
  - type: web
    name: flusk-api
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL # Neon connection string, entered on blueprint creation
        sync: false
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: flusk-redis
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_REFRESH_KEY
        generateValue: true
      - key: CRYPT_SALT
        value: '10'
      - key: TOKEN_EXPIRE_TIME
        value: 1h
      - key: TOKEN_REFRESH_EXPIRE_TIME
        value: 24h
      - key: CORS_ORIGIN
        value: https://flusk.org,https://www.flusk.org
      - key: LOG_LEVEL
        value: '2'

  - type: keyvalue
    name: flusk-redis
    plan: free
    ipAllowList: [] # internal connections only
